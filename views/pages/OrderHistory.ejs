<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Lịch sử đơn hàng - Foodie Express</title>
  <link rel="stylesheet" href="/css/navbar.css" />
  <link rel="stylesheet" href="/css/profile.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
<%- include('../partials/navbar.ejs') %>

<div class="container mt-5 pt-5" style="max-width: 1000px;">
  <div class="card shadow p-4">
    <h3 class="mb-4 text-center">📦 Lịch sử đơn hàng</h3>
    <table class="table table-bordered table-hover align-middle">
      <thead class="table-light text-center">
        <tr>
          <th>Mã đơn</th>
          <th>Thời gian</th>
          <th>Trạng thái</th>
          <th>Phương thức</th>
          <th>Tổng tiền</th>
          <th>Địa chỉ</th>
          <th>Hành động</th>
        </tr>
      </thead>
      <tbody>
        <% if (orders.length === 0) { %>
          <tr><td colspan="7" class="text-center">Bạn chưa có đơn hàng nào.</td></tr>
        <% } else { %>

          <% const sortedOrders = orders.sort((a, b) => {
               if (a.status === 'Delivered' && b.status !== 'Delivered') return -1;
               if (a.status !== 'Delivered' && b.status === 'Delivered') return 1;
               return 0;
             });
          %>

          <% sortedOrders.forEach(order => { %>
            <tr>
              <td class="text-center"><%= order._id %></td>
              <td><%= order.createdAt ? order.createdAt.toLocaleString() : '' %></td>
              <td class="text-center">
                <% if (order.status === 'Pending') { %>
                  <span class="badge bg-warning text-dark">Đang xử lý</span>
                <% } else if (order.status === 'Shipping') { %>
                  <span class="badge bg-info text-dark">Đang giao</span>
                <% } else if (order.status === 'Delivered') { %>
                  <span class="badge bg-primary">Đã giao</span>
                <% } else if (order.status === 'Completed') { %>
                  <span class="badge bg-success">Hoàn tất</span>
                <% } else if (order.status === 'Cancelled') { %>
                  <span class="badge bg-danger">Đã huỷ</span>
                <% } else { %>
                  <span class="badge bg-secondary"><%= order.status %></span>
                <% } %>
              </td>
              <td class="text-center"><%= order.paymentMethod === 'wallet' ? 'Ví nội bộ' : 'COD' %></td>
              <td class="fw-bold text-danger text-end"><%= order.totalPrice.toLocaleString() %> VND</td>
              <td><%= order.address %></td>
              <td class="text-center">
                <button class="btn btn-sm btn-outline-info mb-2" type="button" data-bs-toggle="collapse" data-bs-target="#orderDetail<%= order._id %>">Chi tiết</button>
              </td>
            </tr>

            <tr class="collapse" id="orderDetail<%= order._id %>">
              <td colspan="7">
                <div class="row">
                  <% order.items.forEach(item => { %>
                    <div class="col-md-6 mb-3 d-flex">
                      <img src="<%= item.productId?.imageUrl || '/default.jpg' %>" width="60" height="60" class="rounded me-3 border" />
                      <div>
                        <strong><%= item.productId?.name || 'Sản phẩm đã xoá' %></strong><br>
                        SL: <%= item.quantity %> – Đơn giá: <%= item.price.toLocaleString() %> VND
                      </div>
                    </div>
                  <% }) %>
                  <% if (order.note) { %>
                    <div class="col-12"><em>Ghi chú: <%= order.note %></em></div>
                  <% } %>
                </div>
              </td>
            </tr>

          <% }) %>

        <% } %>
      </tbody>
    </table>
  </div>
</div>

<%- include('../partials/footer.ejs') %>
</body>
</html>
