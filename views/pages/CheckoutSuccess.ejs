<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Order Success - Foodie Express</title>
  
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;600;700;800&display=swap" rel="stylesheet">
  
  <!-- AOS Animation -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />

  <link rel="stylesheet" href="/css/navbar.css" />
  
  <style>
    body {
        font-family: 'Inter', sans-serif;
        background-color: #f8f9fa;
    }
    .font-outfit {
        font-family: 'Outfit', sans-serif;
    }
    .success-card {
        max-width: 550px;
        background: #fff;
        border-radius: 2.5rem;
        padding: 3.5rem 2rem;
        box-shadow: 0 15px 40px rgba(0,0,0,0.06);
        border: 1px solid rgba(0,0,0,0.03);
    }
    .icon-circle {
        width: 100px;
        height: 100px;
        background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
        color: white;
        font-size: 3rem;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        margin: 0 auto 2rem;
        box-shadow: 0 10px 20px rgba(40, 167, 69, 0.2);
    }
    .btn-continue {
        background: linear-gradient(90deg, #ff6347 0%, #fbc02d 100%);
        color: white;
        border: none;
        border-radius: 50px;
        padding: 1rem 2.5rem;
        font-weight: 700;
        font-size: 1.1rem;
        transition: all 0.3s ease;
        box-shadow: 0 8px 20px rgba(255, 99, 71, 0.2);
        text-decoration: none;
        display: inline-block;
    }
    .btn-continue:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 30px rgba(255, 99, 71, 0.3);
        color: white;
    }
    .bg-pattern {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 400px;
        background: linear-gradient(135deg, #fff5f5 0%, #fff 100%);
        z-index: -1;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <%- include('../partials/navbar.ejs') %>

  <main class="flex-grow-1 position-relative d-flex align-items-center justify-content-center py-5 mt-5">
    <div class="bg-pattern"></div>
    
    <div class="container">
        <div class="success-card mx-auto text-center" data-aos="zoom-in">
            <div class="icon-circle mb-4">
                <i class="fas fa-check"></i>
            </div>
            
            <h2 class="display-6 fw-bold font-outfit text-dark mb-3">Order Placed Successfully!</h2>
            <p class="text-muted fs-5 mb-4 px-lg-4">
                Thank you <strong><%= user?.username || 'Customer' %></strong> for choosing Foodie Express. Your delicious meal is being prepared and will be delivered shortly.
            </p>
            
            <div class="order-info bg-light p-3 rounded-4 mb-4 d-inline-block px-4">
                <span class="text-muted small fw-bold text-uppercase">Tracking Order</span>
                <p class="mb-0 fw-bold text-dark">We will send updates to your email.</p>
            </div>
            
            <div class="pt-2">
                <a href="/Menu/listFoods" class="btn-continue">
                    <i class="fas fa-shopping-bag me-2"></i> Continue Shopping
                </a>
            </div>
        </div>
    </div>
  </main>

  <%- include('../partials/footer.ejs') %>